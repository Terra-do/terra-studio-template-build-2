# No keys are needed for the default feeds (heat, air, rain and flood).
# If you add a feed that needs one, put it here as NAME=, copy this file to
# .env.local (git-ignored), and add the same variable in Vercel.
